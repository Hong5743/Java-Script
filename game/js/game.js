const windowEvent = () => {              // 윈도우 이벤트 함수(키, 마우스)
  
}

const init = () => {                     // 프로그램 시작에 필요한 함수
  windowEvent();
}

window.onload = () => {                  // 파일이 실행되고 실행되는 함수
  init();
}  
